This is just a static copy of selected parts of github.com/jovlinger/bin, manually refreshed by make. 

This is the lazy avoidance of git repo trickery or packaging up jovlinger/bin into somethign that can live in requirements.txt.

One caveat is that this stupid scheme only works for "small" bin-aries that have no requirements.txt of their own, or else the referencing code must satisfay those implied requirements manually as well. 
