# Firmware scaffold -- Waveshare Knob-Touch-LCD-1.8 (S3)

PlatformIO Arduino env aimed at the ESP32-S3 half. Encoder GPIOs left unset
until confirmed from schematic / demo (`PIN_ENC_* = -1`).

```bash
cd spinme/firmware/waveshare-knob-touch-lcd-1.8
pio run
```

Hardware docs: [`../../hardware/waveshare-knob-touch-lcd-1.8/`](../../hardware/waveshare-knob-touch-lcd-1.8/).
