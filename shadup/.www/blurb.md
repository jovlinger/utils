Content-addressed file store: deduplicate by SHA-256 into `data/`, symlink tree under `files/`.  
Mainly used for flac serving from sd files on low CPU hardware. 

Also useful for combining poorly-organized photo collections with many duplicated shared albums.
