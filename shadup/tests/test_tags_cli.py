"""CLI tests for tag commands (add / remove)."""

import pytest


def test_tag_add_and_tag_rm_roundtrip_todo() -> None:
    """TODO: subprocess --tag-add / --tag-rm and assert sha_tags in DB."""
    pytest.fail("TODO: integration test for --tag-add and --tag-rm (no coverage yet)")
