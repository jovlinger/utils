[pytest]
# Hard per-test ceiling (slowest refresh pipeline ~2–3s today).
timeout = 15
timeout_method = thread
# Keep collection quiet; durations help catch regressions.
addopts = -q --durations=10
testpaths = tests
