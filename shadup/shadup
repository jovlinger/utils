#!/bin/sh
# Launcher for shadup.py (same venv / PYTHONPATH pattern as ./importtags).
set -eu

SHADUP_LAUNCHER="$0"
# shellcheck disable=SC1091
. "$(CDPATH= cd -- "$(dirname -- "$(readlink -f "$SHADUP_LAUNCHER")")" && pwd)/resolve-venv.sh"

exec python3 -m shadup "$@"
