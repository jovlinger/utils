# Public template for host pytest (local subprocess e2e). Default used by thermo/test/Makefile.
# Copy to config/test.env if you want a private override, or point THERMO_ENV_FILE here.
#
# Must match defaults in test/testcases/test_e2e.py (ports 5001 / 5002).
export DMZ_URL=http://127.0.0.1:5001
export ONBOARD_URL=http://127.0.0.1:5002
