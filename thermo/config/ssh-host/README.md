# DMZ rescue SSH host public keys

Commit public host keys here for known_hosts verification.

Private host keys belong in `thermo/priv/ssh-host/`.

Run `thermo/dmz/install/gen-dmz-rescue-host-keys.sh` to generate private keys
and refresh these public key files.
