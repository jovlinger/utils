# Generated by gen_keys.py / dockertest.sh for compose machine-auth (do not commit priv.pem)
keys/

env/*
!env/
!env/README.md
venv/*
!venv/
!venv/README.md
.venv/*
!.venv/
!.venv/README.md

# Makefile stamp for jovlinger/thermo/test image (see Makefile .image)
.image

# Last dockertest transcript (see dockertest.sh)
.dockertest-last.log
